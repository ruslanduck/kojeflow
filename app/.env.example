AUTH_EMAIL=
AUTH_PASSWORD=

# Airtable — read-only integration with the "Work Hotel" base.
# Leave AIRTABLE_TOKEN empty to run on the committed snapshot in src/data/capture/.
# Create a Personal Access Token at https://airtable.com/create/tokens with scopes
# data.records:read and schema.bases:read, granted access to the Work Hotel base.
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=appB8CoTWVtmH4hbK
